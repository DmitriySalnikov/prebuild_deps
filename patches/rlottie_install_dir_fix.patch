diff --git a/CMakeLists.txt b/CMakeLists.txt
index bd2e432..a986023 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,4 +1,4 @@
-cmake_minimum_required( VERSION 3.3 )
+cmake_minimum_required( VERSION 4.0 )
 
 #declare project
 project( rlottie VERSION 0.2 LANGUAGES C CXX ASM)
@@ -41,7 +41,7 @@ set(LOTTIE_MODULE_PATH "${CMAKE_SHARED_LIBRARY_PREFIX}rlottie-image-loader${CMAK
 configure_file(${CMAKE_CURRENT_LIST_DIR}/cmake/config.h.in config.h)
 
 target_include_directories(rlottie
-    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/inc>
+    #PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/inc
     PRIVATE
         "${CMAKE_CURRENT_BINARY_DIR}"
     )
@@ -135,10 +135,6 @@ if (LOTTIE_ASAN)
     target_link_options(rlottie PUBLIC  -fsanitize=address)
 endif()
 
-if (NOT LIB_INSTALL_DIR)
-    set (LIB_INSTALL_DIR "/usr/lib")
-endif (NOT LIB_INSTALL_DIR)
-
 #declare source and include files
 add_subdirectory(inc)
 add_subdirectory(src)
@@ -149,13 +145,8 @@ if (LOTTIE_TEST)
     add_subdirectory(test)
 endif()
 
-SET(PREFIX ${CMAKE_INSTALL_PREFIX})
-SET(EXEC_DIR ${PREFIX})
-SET(LIBDIR ${LIB_INSTALL_DIR})
-SET(INCDIR ${PREFIX}/include)
-
 CONFIGURE_FILE(${PROJECT_NAME}.pc.in ${PROJECT_NAME}.pc)
-INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}.pc DESTINATION ${LIB_INSTALL_DIR}/pkgconfig)
+INSTALL(FILES ${CMAKE_CURRENT_BINARY_DIR}/${PROJECT_NAME}.pc DESTINATION "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/lib/pkgconfig")
 
 
 #install header
@@ -167,8 +158,8 @@ install(FILES
 
 #install lib
 install( TARGETS rlottie EXPORT rlottie-targets
-         LIBRARY     DESTINATION    ${LIB_INSTALL_DIR}
-         ARCHIVE     DESTINATION    ${LIB_INSTALL_DIR}
+         LIBRARY     DESTINATION    "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/lib"
+         ARCHIVE     DESTINATION    "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/lib"
          INCLUDES    DESTINATION    include
        )
 
@@ -177,7 +168,7 @@ install( TARGETS rlottie EXPORT rlottie-targets
 install( EXPORT rlottie-targets
          FILE          rlottieTargets.cmake
          NAMESPACE     rlottie::
-         DESTINATION   ${LIB_INSTALL_DIR}/cmake/rlottie
+         DESTINATION   "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/lib/cmake/rlottie"
        )
 
 
@@ -191,14 +182,14 @@ write_basic_package_version_file(
 
 configure_package_config_file(${CMAKE_CURRENT_LIST_DIR}/cmake/rlottieConfig.cmake.in
     ${CMAKE_CURRENT_BINARY_DIR}/rlottieConfig.cmake
-    INSTALL_DESTINATION ${LIB_INSTALL_DIR}/cmake/rlottie
+    INSTALL_DESTINATION "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/lib/cmake/rlottie"
 )
 
 #Install the config, configversion and custom find modules
 install(FILES
     ${CMAKE_CURRENT_BINARY_DIR}/rlottieConfig.cmake
     ${CMAKE_CURRENT_BINARY_DIR}/rlottieConfigVersion.cmake
-    DESTINATION ${LIB_INSTALL_DIR}/cmake/rlottie
+    DESTINATION "${CMAKE_INSTALL_PREFIX}/${CMAKE_INSTALL_LIBDIR}/lib/cmake/rlottie"
 )
 
 
